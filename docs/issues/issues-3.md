# [issues-3] 日毎のシフト一覧の表示を改善

## 概要

ShiftListModal の UI の修正を通して UX の改善を行う。
以下のそれぞれ、コミットを分ける。順番に実装を行うこと。

- 種別の表示は不要。その代わり、塾タイプの「事務」タイプシフトの場合は勤務先の末尾に「（事務）」を追加して表示する。
- テーブルの横スクロールが不要になるようにしたい。
  - モーダルの横幅を伸ばす
  - テーブルを使用するのではなくカード形式にする。
    - PC では table-like な表示に
